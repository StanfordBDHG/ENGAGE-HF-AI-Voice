#
# This source file is part of the ENGAGE-HF-AI-Voice open source project
#
# SPDX-FileCopyrightText: 2022 Stanford University and the project authors (see CONTRIBUTORS.md)
#
# SPDX-License-Identifier: MIT
# 

# Required: OpenAI API key for ChatGPT-4o real-time API
OPENAI_API_KEY=<your-openai-api-key>

# Optional: Base64-encoded 32-byte encryption key for encrypting questionnaire responses at rest
# Generate using: openssl rand -base64 32
ENCRYPTION_KEY=<your-base64-encryption-key>

# Optional: Enable internal testing mode (allows multiple surveys per day, reduced KCCQ12 questions)
INTERNAL_TESTING_MODE=true

# Optional: Twilio credentials for recording management (required for /update-recordings endpoint)
TWILIO_ACCOUNT_SID=<your-twilio-account-sid>
TWILIO_API_KEY=<your-twilio-api-key>
TWILIO_SECRET=<your-twilio-api-secret>

# Optional: Custom port for the server (default: 5000 in development, 8080 in Docker)
PORT=5000